import * as errors from "./error.static";
import * as events from "./event.static";
import * as pages from "./page.static";

export { errors, events, pages };
export default { errors, events, pages };
